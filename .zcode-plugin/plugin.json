{
  "name": "gpt-oauth",
  "description": "ChatGPT and xAI Grok OAuth login with an OpenAI-compatible local proxy. Login via browser/device flow, then register provider baseURL http://127.0.0.1:8787/v1 to use GPT and Grok models without an API key.",
  "version": "0.3.1",
  "author": { "name": "kvu" },
  "commands": ["commands"],
  "mcpServers": {
    "gpt-oauth": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/server.js"]
    }
  }
}
